# React frontend environment variables (example)
# Copy to your local .env and fill values as needed.

# Alpha Vantage API key (OPTIONAL)
# - If set, LIVE mode (prices/history) is enabled.
# - If missing, the app will default to MOCK so preview/CI can run without secrets.
REACT_APP_ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here
